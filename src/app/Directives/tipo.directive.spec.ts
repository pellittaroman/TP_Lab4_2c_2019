import { TipoDirective } from './tipo.directive';

describe('TipoDirective', () => {
  it('should create an instance', () => {
    const directive = new TipoDirective();
    expect(directive).toBeTruthy();
  });
});
